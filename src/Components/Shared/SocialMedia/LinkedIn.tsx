import linkedIn from "/linkedIn.svg";

const LinkedIn = ({ className }: { className?: string }) => {
  return <img src={linkedIn} className={className} alt="LinkedIn logo" />;
};

export default LinkedIn;
